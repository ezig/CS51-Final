(ns npuzzles.core
	(:require [npuzzles.puzzle :as puzzle])
	(:require [npuzzles.astar :as astar])
  	(:gen-class))

(defn -main
	 "I don't do a whole lot ... yet."
 	[& args]
 	(println "Hello, World!"))
